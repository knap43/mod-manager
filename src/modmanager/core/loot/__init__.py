"""LOOT-compatible plugin sorting.

A native reimplementation of LOOT's sorting algorithm
(https://loot-api.readthedocs.io/en/latest/api/sorting.html) that uses the
community masterlists LOOT maintains (https://github.com/loot), so no Windows
tools are needed.
"""
